<%= simple_form_for [:admin, product] do |f| %>
  <section class="form-inputs">
    <%= f.input :name %>
    <%= f.input :price %>

    <%= f.input :rentable %>
    <%= f.input :purchasable %>
  </section>

  <section class="form-links">
    <%= f.submit %>
  </section>
<% end %>
